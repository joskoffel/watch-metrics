// TODO: BaselineTracker (7d/28d rolling window) lands in a later step.
// Deliberately left as an empty protocol — no fields added without a
// justified use, per project workflow (see docs/project-status.md).
public protocol Baseline {
}
